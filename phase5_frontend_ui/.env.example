# Backend API Base URL
# The frontend will only communicate with the backend API, never directly accessing JSON files
# Development: http://localhost:8000
# Production: Your Railway/Render backend URL (e.g., https://your-backend.railway.app)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
